using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

namespace Drills
{

    public class InitConnection : MonoBehaviour
    {
        // Connection to initialize or uninitialize.
        [SerializeField]
        private GameObject connection;

        // Other endpoint of the connection.
        [SerializeField]
        private BlockContainer otherblock;


        // Use this for initialization
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }

}